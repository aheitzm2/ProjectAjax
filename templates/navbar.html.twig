<!--Navbar-->
<nav class="navbar navbar-expand-lg navbar-dark primary-color">

    <!-- Navbar brand -->
    <a class="navbar-brand" href="{{ path('Home.index') }}">City Locate</a>

    <!-- Collapse button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#basicExampleNav"
            aria-controls="basicExampleNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content -->
    <div class="collapse navbar-collapse" id="basicExampleNav">

        <!-- Links -->
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('Home.index') }}">Accueil
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('Admin.menu') }}">Menu admin
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                {% if not app.user %}
                    <a href="{{ path('Security.login') }}" class="nav-link"><i class="fas fa-sign-in-alt"></i> Connexion</a>
                {% else %}
                    <a href="{{ path('Security.logout') }}" class="nav-link"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                {% endif %}
            </li>
            <li class="nav-item">
                <a href="{{ path('Security.registration') }}" class="nav-link"><i class="fas fa-user"></i> Inscription</a>
            </li>
        </ul>
    </div>
    <!-- Collapsible content -->

</nav>